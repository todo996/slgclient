// /**http接口Thiết lập*/
// //Tài khoảnĐăng ký

import { _decorator } from 'cc';
const HttpConfig = {
    register: {
        name: "register",
        url: "/account/register"
    },
}
export { HttpConfig };
